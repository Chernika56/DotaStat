@model IEnumerable<Dota2Stat.Models.DB.Team>

@{
    ViewData["Title"] = "Index";
}

<h1>Teams</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
                Rating
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TmName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TmRating)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TmId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TmId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TmId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
